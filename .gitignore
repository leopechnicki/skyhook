node_modules/
npm-debug.log*

# package-lock.json is intentionally COMMITTED (was ignored until 2026-09-11).
# CI runs `npm ci`, which requires it, and an unpinned devDependency range
# means a future Playwright release can turn the build red with no code
# change behind it - a regression signal nobody caused.

# Test artifacts (regenerated by `node test/smoke.mjs`)
test/screenshots/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
